# RULES

このリポジトリで作業する全員に適用される。人間にも、どの AI にも、どのセッションにも。
`CLAUDE.md` から参照されているので、Claude Code は自動で読む。

---

## 0. この道具の設計原則

実写動画に自動でモザイクをかける道具。日本の刑法175条対応が目的。

**モザイクが漏れる方向の壊れ方が致命的で、塗り過ぎ側は許容できる。**

- 判断がつかないときは塞ぐ。捨てない。止める。
- **黙って素通しを作らない。** 不整合を検出したらエラーで止めるか、最低でも明示的に警告する。
- 「1フレームでも漏れたら意味がない」という性質の道具である。平均や割合で成功を語らない。

---

## 1. 証拠が証言に優先する（artifact > testimony）

**「対応した」「テストが通った」は AI の証言にすぎない。**
証言ではなく、**その場で取得した生の実行結果だけ**を根拠として認める。

### 1.1 同一ターンの生鮮証拠のみ

「対応済み」と書く直前に、実コマンドの出力を貼る。
**記憶・過去ターン・別セッションの結果を根拠にしない。**

悪い例:
> テストは先ほど通りました。修正は完了しています。

良い例:
> ```
> $ .venv/Scripts/python.exe tests/test_review.py
> ...
> すべて通過
> ```
> この出力を得たので、`tests/test_review.py` は現時点で通っている。

### 1.2 要約でなく生ログを引用する

「〜を確認しました」ではなく、**実行ログの該当行そのもの**を貼る。
行を選んで貼るのは可。書き換えるのは不可。省略したら省略したと書く。

### 1.3 未確認は未確認と書いてよい

**「わからない」「まだ検証していない」は失敗ではない。**
埋め合わせの捏造のほうがはるかに悪い。

- 実行していない主張には「未実行」と明記する。
- 再現できなかったものは「再現できなかった」と書き、どこまで試したかを列挙する。
- 推測で「たぶん大丈夫」と書かない。

### 1.4 反証を要求する

レビューを頼むときは「これで合ってる?」ではなく
**「この主張を崩す証拠を探せ。見つからなければ出すな」**と頼む。

自分の修正についても同じ。**壊れることを再現してから直し、再現しなくなったことを確認する。**
この順序を守らない修正は、直っているかどうか誰にも分からない。

### 1.5 書いた本人と別の文脈でクロスチェックする

最終報告の主張と実行ログの整合性だけを見る、**独立した検証パス**を挟む。
書いた側の物語に引きずられない位置で見る。

- 実装した者が自分の実装を検証しない。
- 検証担当はファイルを編集しない。読むことと実行することだけをする。
- 検証担当には「報告を鵜呑みにするな、自分で再現しろ」と明示的に指示する。

### 1.6 タスクを小さく割る

特大の変更は、**AI の内的な認識と実コードの距離が開きやすい**。
1つの PR で1つのことをする。独立して戻せる単位で刻む。

---

## 1.7 `git stash` は worktree 間で共有される。並行作業では使わない

**`git stash` のスタックはリポジトリ1個につき1本で、worktree ごとに分かれていない。**
複数の worktree で別々のエージェントが同時に作業していると、あるエージェントの
`git stash push` / `pop` が別の worktree の未コミット変更を巻き込み、
黙って混入させたり消したりする。実際に issue #2 の作業中に issue #33 の変更が
混入し、気づかず `pop` していたら #2 の修正が消えていた（実測・2026-08-24）。

**「修正を一時的に外して壊れることを確認する」（RULES.md 1.4）には `git stash` を使わない。**
代わりに `git diff > /tmp/x.patch && git checkout -- <file>` のように明示的にパッチを
退避してから戻す、または `git worktree` 自体が使い捨てなので該当ファイルを一時的に
直接編集して確認後 `git checkout -- <file>` で戻す。

---

## 2. テストに関する規律

- **落ちないテストは意味がない。** 回帰テストを足したら、**修正を一時的に外すと実際に落ちる**
  ことを確認してから「テストを足した」と書く。
- テストが検証しているつもりのものを本当に検証しているか、たまに疑う。
  実例: `test_geometric_filter_drops_huge_boxes` は引数をどう変えても常に通っており、
  名前どおりの検証を1年近くしていなかった。
- 合成素材の落とし穴を知っておく。**`ffmpeg -f lavfi -i testsrc2` は奇数サイズ指定を
  黙って偶数に丸める。** 奇数解像度を試したいなら `color=` か `nullsrc=` を使う。

### そのテストが実行されているか確かめる

**アサーションが正しくても、実行されなければ無いのと同じ。**

PR #43 は `session_overrides()` が4キーを転送することを見るテストを足した。
アサーションは正しかった。**しかし `if __name__ == "__main__":` ブロックより後に
定義されていた。**

このリポジトリのテストは pytest ではなく素の Python スクリプトで、
`globals()` を走査して `test_` 関数を集める。**Python はトップダウンに実行するので、
収集が走る時点でまだ定義されていない関数は集まらない。**

```
32 件のテストを実行            <- 足したテストを含まない件数
M7変異（4キーを落とす）        -> すべて通過（検出できず）
単独変異4本（1キーずつ落とす） -> すべて通過（検出できず）
関数を直接呼ぶと               -> FAIL: margin_scale が落ちた
```

**確かめ方は2つ。どちらも変異を当てる前にやる。**

1. **スクリプトとして実行したときの件数が増えたことを見る。** 分岐点と手元で数えて比べる。

   ```
   PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe tests/test_webapp.py
   -> 「40 件のテストを実行」の数字を読む
   ```

   **`importlib` でモジュールとして読み込んで `test_` 関数を数えてはいけない。**
   モジュールとして読むと `__name__` が `"__main__"` にならないので収集ブロックが動かず、
   **配置に関係なく全部定義されてしまう。** 実際にこれで一度だまされた
   （配置ミスのある PR とマージ済みの master で、どちらも同じように数字が増えて見えた）。

2. **わざと失敗するアサーションを一時的に入れて、実際に落ちることを見る。**
   落ちなければ、そのテストは走っていない

**行番号で直接見るのがいちばん速い。** `if __name__` の行より前にあるか数えるだけ。

### フロントエンドは、変異とテストのあいだにビルドを挟む

`tests/test_frontend.mjs` は **`.ts` ではなく `frontend/build/logic.mjs`（ビルド済み）を読む。**

```
const L = await import("file://" + LOGIC.replace(/\/g, "/"));
```

**`.ts` を書き換えただけでは、テストには何も届かない。**
変異を当てて `node tests/test_frontend.mjs` を走らせると**必ず「すべて通過」になる。**

実際に一度だまされた（PR #85）。2種類の変異がどちらも素通りし、
`cd frontend && node build.mjs` を挟んだら 3件と1件が落ちた。

**「変異が検出されない」と「変異が届いていない」を区別できないのは、
テストが収集されていないのと同じこと。** 順序は必ず:

```
1. .ts を書き換える
2. cd frontend && node build.mjs
3. node tests/test_frontend.mjs
4. git checkout -- <file> して 2. をもう一度（復元もビルドが要る）
```

**4 を忘れると、次のテストが変異したバンドルで走る。**

**変異テストは「落ちること」を見る。走っていないテストは、変異を当てても落ちないので
『変異が検出されない』と『テストが走っていない』を区別できない。**
先に走っていることを確かめる。

---

## 2.1 計測そのものを疑う

**測り方が壊れていると、正しい結論も間違った結論も同じ顔で出てくる。**
実際に踏んだものを挙げる。

### 表示が真実を隠す

端末に直接 print すると、stdout 側のエンコーディングが壊れた文字を黙って置き換え、
**正常に見えることがある**。ログの文字化けを調べていて、一度これで見落としかけた。

壊れた文字列を調べるときは `repr()` か符号点で見ること。

```python
sur = [hex(ord(c)) for c in t if 0xD800 <= ord(c) <= 0xDFFF]
```

### 判定基準が片側に外れていないか確かめる

「モザイクがかかっているか」を**ブロック1個ぶんの標準偏差が元映像比 0.5 未満か**で
判定した例があった。サンプルした1ブロックが実際に塗られた領域の外に落ちると、
**塗られているのに素通しと判定する**。実測で 60/60 フレームを誤判定した。

その代わりに**元映像との平均絶対差**を推したが、**これも素材によっては使えない。**
実素材（1080p、crf 16、モザイク領域が 200x200px 程度）で測ると分布が完全に重なる:

| 指標 | 未塗装 | 塗装 | 最良の単一閾値での総誤り |
|---|---|---|---|
| フレーム全体の平均絶対差 | p50 4.2 / max 5.1 | p50 4.4 / min 1.3 | 41.9% |
| 16x16ブロックの最大平均絶対差 | p50 10.9 / max 23.2 | p50 25.3 / min 8.1 | 14.8% |

再エンコードの粒状ノイズが効き、**塗装フレームのほうがフレーム平均差分が低いことすらある。**
合成素材で 25〜40倍に分離したのは、モザイク領域が画面に占める割合が大きかったから。

**実際に効いたのは、差分ではなくモザイクの構造そのものを見る基準だった。**
`render.pixelize_plane` は 20px 格子にスナップして各セルを平均色1色に潰すので、
**モザイク領域はセル内分散がゼロになる**。画面を 20x20 の格子で密に見て、
「元のセル内 std >= 6 かつ 出力のセル内 std < 元の 0.35 倍」のセルを数える。

**この基準の閾値を、あとで自分たちで踏み抜いた。** 最初は
無作為60窓 × 100フレーム = 5,900 の標本で校正し、「潰れたセルが2個以上ならモザイクあり」
（未塗装 2802 フレームで誤判定 0、塗装側の第1パーセンタイルは閾値の8倍）と結論した。

**全 55,303 フレームを密に走査すると、その閾値は危険側に外れる**（issue #13 / PR #83）:

```
min_cells=2: 未塗装のはずが「あり」と判定  6/16660 (0.036%)   見逃し 100/38643 (0.259%)
min_cells=3: 未塗装のはずが「あり」と判定  0/16660 (0.000%)   見逃し 127/38643 (0.329%)
```

**標本校正では一度も踏まなかった6件を、全数走査は踏む。**
危険側＝素通しを見逃す向きなので、この道具では使えない。**閾値は3。**

教訓は4つ。

1. **既知の基準を借りてこない。** 素材が変われば分離も変わる。**毎回、自分の素材で
   分布を測ってから閾値を決める**
2. **標本ではなく密に見る。** 1ブロックの標本は塗装領域の外に落ちる。
   **そして「密に見る」は窓の数を増やすことではない。全部見ることを指す。**
   60窓 × 100フレームは 5,900 フレームで、それでも 55,303 の 10.7% でしかなかった
3. **差分より構造。** 「何が起きたか」を直接見られるなら、そちらのほうが頑健
4. **ここに書いてある数字も疑うこと。** この節の閾値は一度間違っていた。
   **`RULES.md` に書いてあることは、借りてよい理由にならない**

判定基準を決めたら、**分離の幅を数字で確かめること。** 閾値に対する余裕が数%しかない
基準は、そのうち必ず外れる。そして**どちら側に外れるか**を意識すること。
この道具では素通し側に外れる基準は使えない。

### 数字が一致しすぎるときは測り方を疑う

先行する実測と自分の測定が完全に一致したとき、それは再現性の証拠かもしれないし、
**先行の数字を見てから測って合わせに行った**結果かもしれない。

検証するなら、**先行の数字を見る前に自分の測り方で測る。** 測ってから突き合わせる。

### 条件を逐次で測ると、順序が結果になる

GPU や I/O を共有する環境で条件を順番に測ると、**時間の差が条件の差に見える**。
「非正方入力は正方より速い」という結論が、インターリーブして測り直したら
符号が逆転してノイズの内側だった、という例があった。

比較する条件は**同じフレームを交互に回す**こと。


---

## 3. 変更を出すときの形

### 3.1 コミット

- **`Co-Authored-By: Claude` を付けない。**
- **装飾絵文字を使わない**（🎯 ⚠️ ✅ など）。矢印記号は可。
- コメントと文書は日本語。既存の密度に合わせる。
- メッセージは「何を直したか」ではなく「何が壊れていたか」を書く。
  読む人が知りたいのは、その変更が無かったら何が起きていたか。

### 3.2 PR

`.github/pull_request_template.md` に従う。証拠欄を埋めずに出さない。

### 3.3 Issue

`.github/ISSUE_TEMPLATE/` に従う。
**「〜が怪しい」で終わらせず、踏む手順を書く。** 踏めていないなら踏めていないと書く。

### 3.4 merge は受け入れ条件に照らして行う

**人間が決めるのは受け入れ条件であって、1件ごとの可否ではない。**
条件は AI が起草して人間に確認を求めてよい。確認の取れた条件に照らして、AI が merge してよい。

**受け入れ条件（2026-08-24 確認）**

必須:

1. **独立検証を通っている。** 実装した者以外が、報告を鵜呑みにせず自分で再現・反証を試みた
2. **回帰テストが、修正を外すと実際に落ちる。** 変異は**複数**試すこと。
   1種類だけだと `TypeError` や `ImportError` で落ちているだけの退化した確認を見逃す
3. **漏れる方向の新規リスクが無い。** 塗り過ぎ側に外れるなら可
4. **検証が「マージ前」と指定した条件が解消済み**
5. 全テストが通る（`weights/` 不在で落ちる既知の1件を除く）

却下:

- 検証が「現状のままでは入れない方がよい」と判定した -> 直してから再検証
- 主張を支える生ログが PR に無い

速い経路（検証を軽くしてよい）:

- 文書・ツールの追加のみで、既存の挙動を1ビットも変えない PR

**issue の close は merge に任せる。** `Closes #N` を PR 本文に書けば merge で閉じる。
手で close しない。

**自分の PR を自分で検証しない**（1.5）。実装したセッションとは別の文脈で検証すること。

**別のセッションが出した未 merge の PR がある issue には重複 PR を作らない。**
既存 PR を読み、重複になる場合はその旨を issue に短く返信するだけにする。

---

## 4. 環境の制約（実測済み）

- **`git push` は固まることがある。** 2026-08-24 時点では通った（`89e9715..6ad436a`）。
  固まったら `gh` の API 経由（Git Data API でコミットを生成）で回避できる。
- **arxiv からの直接ダウンロードは固まる。** WebSearch と WebFetch は通る。
  abs ページか ar5iv、Semantic Scholar を使う。
- Windows / PowerShell と Git Bash の両方が使える。それぞれ構文が違う。
- Python は `.venv/Scripts/python.exe`。`PYTHONIOENCODING=utf-8` を設定しないと
  日本語の出力が化ける。
- GPU は AMD の DirectML（`onnxruntime-directml`）。**CUDA 前提のものは動かない。**
- `data/myvideo5` `data/bench3` は既存の作業データ。**読むだけ。書き換えない。**
- ローカルで動かしているサーバは、断りなく止めない。

---

## 5. 作業の分担

分割できる実作業は既定でサブエージェント（並列）かワークフローに投げる。
手作業で抱え込まない。

モデルの割り当て（2026-08-25 更新）:

**三層で回す。** 上に行くほど高い。使う場所を間違えないこと。

| 層 | 役割 | モデル |
|---|---|---|
| 上級 | 全体のタスク管理、マージ判定、優先順位 | 本体セッション（最上位） |
| 中級 | 検証、設計判断、反証 | `sonnet` |
| 下級 | 実装、機械的な修正、下調べ、issue の整理 | `haiku`（重ければ `sonnet`） |

**`opus` をサブエージェントに指定しない。** 本体セッションが上級層を担う。

エージェントに渡せる指定は `opus` / `sonnet` / `haiku` / `fable` のみ。

**モデルを軽くしても、独立検証と変異テストの要件（3.4）は下げない。**
軽いモデルほど、指示に踏むべき手順と落とし穴を具体的に書くこと。
今夜の実績では、issue の整理は haiku で 76k トークン、検証は 120〜146k かかっている。
**トークン量はモデルでなく「何を読ませ、何を実行させるか」で決まる。**

判定・分類・レビューは独立パネル（複数エージェント）＋合議で。
**モデルを軽くしても、独立検証と変異テストの要件（3.4）は下げない。**
軽いモデルほど、指示に踏むべき手順と落とし穴を具体的に書くこと。

---

## 6. 書かないこと

- 内部の作業過程（「以前は」「撤回して」「照合して確定した」）を成果物に書かない。
- 査読番号・内部符牒・エージェント名を成果物に書かない。
- 「再ダウンロードできる」を理由にコンテンツの実体を消さない。整理は移動・集約で。
