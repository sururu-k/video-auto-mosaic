# AMD GPU (DirectML) 用。NVIDIA なら onnxruntime-gpu、CPU のみなら onnxruntime に差し替える
onnxruntime-directml>=1.20
opencv-python-headless>=4.9
numpy>=1.26
scipy>=1.11          # トラックと検出の対応付けを線形割当で解く（無くても貪欲で動く）

# Web アプリ（automosaic.webapp）。既存の CLI とレビュー UI はこれ無しでも動く
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9   # <form> からのアップロードを受けるのに要る
